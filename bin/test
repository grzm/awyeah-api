#!/usr/bin/env bash

set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"

cd "${repo_root}"
echo "bb tests"
bb --classpath $(clojure -Spath -A:bb:dev:test) \
   --main com.grzm.awyeah.test/run-tests

echo "clj tests"
clj -X:dev:test:clj
